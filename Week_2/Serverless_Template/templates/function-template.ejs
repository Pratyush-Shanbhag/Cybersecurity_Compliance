'use strict';

module.exports.<%= functionName %> = async (event, context) => {
    return {
        statusCode: 200,
        body: JSON.stringify({
            message: 'Function executed successfully',
            input: event,
        }),
    };
};